export default function Settings() {
  return (
    <div>
      <h1 style={{ fontSize: '1.5rem', fontWeight: 700, marginBottom: '1rem' }}>Settings</h1>
      <p style={{ color: 'var(--color-text-secondary)' }}>ON24 credentials and sync configuration — coming in Phase 5</p>
    </div>
  )
}
